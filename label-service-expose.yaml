apiVersion: v1
kind: Service
metadata:
 name: label-service-expose
 namespace: map-dev
spec:
 type: NodePort
 ports:
  - port: 80
    targetPort: 8009
    protocol: TCP
 selector:
  app: label-service
